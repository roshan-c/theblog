# robots-txt

Configures crawler rules for gartblog.xyz via `/robots.txt`.

- Includes default `User-agent: *` policy
- Includes explicit AI crawler blocks for GPTBot, OAI-SearchBot, Claude-Web, Google-Extended
- Declares Content-Signal directives
- References sitemap
